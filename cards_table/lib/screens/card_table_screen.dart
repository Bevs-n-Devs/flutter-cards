import 'package:flutter/material.dart';

class CardsTableScreen extends StatelessWidget {
  const CardsTableScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}